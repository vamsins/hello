name: my fiest githgub pipeline
on: 
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello  world
        run: echo "hello world"
      - name: ckeckout background
        uses: actions/checkout@v4
      - name: list
        run: ls -a
       
  

